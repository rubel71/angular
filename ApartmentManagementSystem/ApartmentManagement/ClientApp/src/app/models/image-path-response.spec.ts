import { ImagePathResponse } from './image-path-response';

describe('ImagePathResponse', () => {
  it('should create an instance', () => {
    expect(new ImagePathResponse()).toBeTruthy();
  });
});
